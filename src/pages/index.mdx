---
navigationLabel: HOME
---

<!-- For index -->

import { Link as GatsbyLink } from "gatsby";
import { mix } from "@theme-ui/color";
import { PostCard } from "../components/post-card";
import { ProfileInfo } from "../components/profile-info";
import { NewsletterForm } from "../components/newsletter-form";

<Alert variant="success">
  Hi! I'm Hai [はい], Welcome to my blog. (หวัดดี 👋 )
</Alert>
<Divider />
<Divider />

<!-- 
<Button variant='success'>View this repository</Button>
<Divider /> -->

<!-- <ProfileInfo /> -->

<Divider />
<Divider />

## hello world

หวัดดี 👋 บล็อคนี้จัดทำขึ้นมาเพื่อจุดประสงค์แบ่งปันความรู้ แชร์ความ ~~ไร้สาระ~~ รู้ต่างๆ
ซึ่งก่อนอื่นต้องบอกก่อนว่าบล็อคแห่งนี้ไม่มีทีมงาน พัฒนาและเขียนบล็อคโดยผมคนเดียวเท่านั้น
โดยเนื้อหาในบล็อคนี้ก็จะมีหลากหลายตาม ~~ความไร้สาระ~~ tag ด้านล่างเลย
หลักๆแล้ว ก็จะเกี่ยวข้องกับเรื่องที่ผมพอมีประสบการณ์และสนใจ หรือไม่ก็เรื่องไรสาระไปวันๆ 😅 แฮะ แฮะ แฮะ!<hr />
อะแฮ่ม! 👏 โดยจุดประสงค์ที่แท้จริงของบล็อคนี้คือ เป็นการบันทึกความทรงจำเพื่อให้ตัวเองกลับมาอ่านในภายหลัง
`ถ้าไม่ขี้เกียจอะนะ` และแชร์ความรู้อันน้อยนิดที่มี ให้เพื่อนๆรอบข้างได้มาอ่านบ้าง
นะไอ้พวกโง่ 🍌 แค่นั้นแหละ. หวังว่าบล็อคนี้จะเป็นประโยชน์ต่อผู้อ่านไม่น้อยก็น้อยมาก
ฝากเนื้อฝากตัวด้วยครับ สัญญาว่าจะพยายามเขียนแต่เรื่องที่มัน ~~ไร้สาระ~~ เป็นประโยชน์ครับ.

<Divider />
<Divider />

<!-- Contact and Tags -->

<Grid>

<Box>

## blog topics

<Divider />

<SourceTags>
  {(tags) => (
    <Box>
      {tags
        .sort((a, b) => b.percent - a.percent)
        .map((tag, index) => {
          const { name, count, percent } = tag;
          return (
            <Badge
              key={index}
              variant="primary"
              sx={{
                mr: 2,
                mb: 2,
                color: mix("secondary", "primary", `${index / tags.length}`),
                borderColor: mix(
                  "secondary",
                  "primary",
                  `${index / tags.length}`
                ),
              }}
            >
              {name}
            </Badge>
          );
        })}
    </Box>
  )}
</SourceTags>

</Box>
</Grid>

<!-- News letter -->

<NewsletterForm />

<!-- Posts -->

# Latest Posts



> 📝 ``เอาล่ะ.. มาอ่านโพสต์ล่าสุดกันเลยยยยยยยยยยยยยย``

<Divider />
<Divider />

<SourceList filter="posts">
  {(posts) => (
    <Grid
      sx={{
        gridTemplateColumns: ["1fr", "1fr", "1fr 1fr"],
      }}
    >
      {posts
        .filter((edge) => !edge.node.frontmatter.isPrivate)
        .reduce((posts, post) => {
          return post.node.frontmatter.pinned
            ? [post, ...posts]
            : [...posts, post];
        }, [])
        .splice(0, 4)
        .map((edge, index) => {
          const {
            featuredImageUrl,
            frontmatter: { title, tags, date, dateModified, pinned },
            excerpt,
            fields: { slug },
          } = edge.node;
          return (
            <PostCard
              key={index}
              title={title}
              featuredImageUrl={featuredImageUrl}
              tags={tags}
              date={date}
              dateModified={dateModified}
              excerpt={excerpt}
              slug={slug}
              pinned={pinned}
            />
          );
        })}
    </Grid>
  )}
</SourceList>
<Divider />

<!-- Read more -->

<Flex
  sx={{
    justifyContent: "center",
  }}
>
  <Box>
    <Button as={GatsbyLink} variant="ghost" to="/posts" >
      <span style={{fontSize: '1.3rem'}}>อ่านโพสต์ทั้งหมด{" "}
      <Box as="span" role="img" aria-label="pencil">
        ✏️
      </Box>
      </span>
    </Button>
  </Box>
</Flex>

<Divider />
