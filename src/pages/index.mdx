---
navigationLabel: HOME
---

<!-- For index -->

import { Link as GatsbyLink } from "gatsby";
import { mix } from "@theme-ui/color";
import { PostCard } from "../components/post-card";
import { ProfileInfo } from "../components/profile-info";
import { NewsletterForm } from "../components/newsletter-form";

<!-- For dashboard -->

import { PostsByMonthChart } from "../components/posts-by-month-chart";
import { PostsByDayChart } from "../components/posts-by-day-chart";
import { WordCountChart } from "../components/word-count-chart";
import { TagPercentChart } from "../components/tag-percent-chart";

<Alert variant="success">Hi!  I'm Hai [はい], Welcome to my blog. (หวัดดี 👋 )</Alert>
<Divider />
<Divider />

<!-- <ProfileInfo /> -->

<Divider />
<Divider />

## intro

Well hey there! 👋 I'm Paul and a long long time ago I was a designer but after a few years of pixel pushing I realised
code is just better! I’ve held various positions in and around advertising and web development in both permanent and
contract positions since 2005, and I once co-founded and ran an award winning digital production agency called Super
natural based in London.

<Divider />

<!-- Contact and Tags -->

<Grid
  sx={{ gridTemplateColumns: ['1fr', '1fr 1fr']}}
>
<Box>

## contact

<Heading as="h3" variant="styles.h3" marginBottom={1}>Email</Heading>
<Link
  href="mailto:rabbit.bot@outlook.com"
  target="_blank"
  variant="styles.a"
  rel="noopener"
>
  rabbit.bot@outlook.com
</Link>
<Divider />

<Heading as="h3" variant="styles.h3" marginBottom={1}>GitHub</Heading>
<Link
  href="https://github.com/alohadancemeow"
  target="_blank"
  variant="styles.a"
  rel="noopener"
>
  https://github.com/alohadancemeow
</Link>
<Divider />

<Heading as="h3" variant="styles.h3" marginBottom={1}>Steam</Heading>
<Link
  href="https://steamcommunity.com/profiles/76561198144315513/"
  target="_blank"
  variant="styles.a"
  rel="noopener"
>
  https://steamcommunity.com/profiles/76561198144315513
</Link>
<Divider />

</Box>

<Box>

## blog topics

<Divider />

<SourceTags>
  {(tags) => (
    <Box>
      {tags
        .sort((a, b) => b.percent - a.percent)
        .map((tag, index) => {
          const { name, count, percent } = tag;
          return (
            <Badge
              key={index}
              variant="primary"
              sx={{
                mr: 2,
                mb: 2,
                color: mix("secondary", "primary", `${index / tags.length}`),
                borderColor: mix(
                  "secondary",
                  "primary",
                  `${index / tags.length}`
                ),
              }}
            >
              {name}
            </Badge>
          );
        })}
    </Box>
  )}
</SourceTags>

</Box>
</Grid>

<!-- News letter -->

<NewsletterForm />

<!-- Posts -->

# Latest Posts

<Divider />

<SourceList filter="posts">
  {(posts) => (
    <Grid
      sx={{
        gridTemplateColumns: ["1fr", "1fr", "1fr 1fr"],
      }}
    >
      {posts
        .filter((edge) => !edge.node.frontmatter.isPrivate)
        .reduce((posts, post) => {
          return post.node.frontmatter.pinned
            ? [post, ...posts]
            : [...posts, post];
        }, [])
        .splice(0, 4)
        .map((edge, index) => {
          const {
            featuredImageUrl,
            frontmatter: { title, tags, date, dateModified, pinned },
            excerpt,
            fields: { slug },
          } = edge.node;
          return (
            <PostCard
              key={index}
              title={title}
              featuredImageUrl={featuredImageUrl}
              tags={tags}
              date={date}
              dateModified={dateModified}
              excerpt={excerpt}
              slug={slug}
              pinned={pinned}
            />
          );
        })}
    </Grid>
  )}
</SourceList>
<Divider />

<!-- Read more -->

<Flex
  sx={{
    justifyContent: "center",
  }}
>
  <Box>
    <Button as={GatsbyLink} variant="ghost" to="/posts">
      More posts{" "}
      <Box as="span" role="img" aria-label="pencil">
        ✏️
      </Box>
    </Button>
  </Box>
</Flex>

<Divider />
<Divider />

<!-- Dashboard section -->

# Dashboard

Here are the totals, counts and other statistics calculated from posts and tags used around my blog

<Divider />

## tags by usage

<Grid sx={{gridTemplateColumns: ['1fr', '1fr 1fr'], gridGap: 3}}>

<TagPercentChart year={0} color={"primary"} />

<TagPercentChart year={-1} color={"secondary"} />

</Grid>

<Divider />
<Divider />

## posts by day

<Grid sx={{gridTemplateColumns: ['1fr', '1fr 1fr'], gridGap: 3}}>

<PostsByDayChart
  config={{
    year: 0,
    color: "primary",
  }}
/>

<PostsByDayChart
  config={{
    year: -1,
    color: "secondary",
  }}
/>

</Grid>

<Divider />
<Divider />

## posts by month

<Grid sx={{gridTemplateColumns: ['1fr', '1fr 1fr'], gridGap: 3}}>

<PostsByMonthChart
  config={{
    year: 0,
    color: "primary",
  }}
/>

<PostsByMonthChart
  config={{
    year: -1,
    color: "secondary",
  }}
/>

</Grid>

<Divider />
<Divider />

## word count statistics

<Grid sx={{gridTemplateColumns: ['1fr', '1fr 1fr'], gridGap: 3}}>

<WordCountChart
  title="Average word count"
  dimension="words"
  config={{ year: 0, color: "primary" }}
/>

<WordCountChart
  title="Average word count"
  dimension="words"
  config={{ year: -1, color: "secondary" }}
/>

</Grid>
